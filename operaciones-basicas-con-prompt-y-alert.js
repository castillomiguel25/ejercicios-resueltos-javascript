// 1)  escribe un programa que muestre un aviso donde el usuario ingresa un numero,
//  luego muestra otro aviso e ingresa otro numero, finalmente
//   el resultado llega con el resultado de la suma de los 2

 let resultado;
 let age = prompt ('escribe un numero');
 let age2 = prompt ('escribe otro numero');

 resultado = (parseInt(age) + parseInt(age2));
 alert(` tu resultado es ${resultado}` );


// 2) crea un programa que reciba 3 numero con aviso, 
// y regrese el promedio de ello con una alerta

 let resultado1;
 let result = prompt ('escribe un numero');
 let result2 = prompt ('escribe otro numero');
 let result3 = prompt ('escribe otro numero');
 resultado1 = ((parseInt(result) + parseInt(result2) + parseInt(result3))/3);

 alert(` tu resultado es ${resultado1}` );

// 3) crea un programa que pida con prompt nombre y edad
// con esto mostrar un alerta similar a hola juan tienes 25 anos

  let name = prompt ('escribe tu nombre');
  let ages = prompt ('escribe tu edad');
  alert(`hola ${name} tienes ${ages} anos`);



// 4) si a = [1,2,3] crea un script que determine un alert de tipo de dato a.

 a = [1,2,3]
 alert(typeof a)

// 5) pedir un numero a un usuario a traves de un prompt y luego dividirlo por 10, por ejemplo: 5 /10 igual a 0.5

 var resulta
 var date = prompt('escribe un numero');

 var resulta = (( date )/10)

 alert(result)


